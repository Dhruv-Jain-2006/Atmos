"""Deterministic and statistical analysis.

Everything here is a pure transform of measured data. The LLM is not a detector
(CLAUDE.md, "LLM Philosophy"), so nothing in this package may call one.
"""
